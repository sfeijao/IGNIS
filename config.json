{
  "note": "Este ficheiro é legado e não deve conter segredos. Use variáveis de ambiente (.env). Ver .env.example.",
  "token": "${DISCORD_TOKEN}",
  "clientId": "${CLIENT_ID}",
  "clientSecret": "${CLIENT_SECRET}",
  "guildId": "${GUILD_ID}",
  "channels": {
    "verification": "${VERIFICATION_CHANNEL_ID}",
    "logs": "${LOGS_CHANNEL_ID}",
    "status": "",
    "solicitarTag": "",
    "comandosAdm": "",
    "cargoLogs": "",
    "updates": "${UPDATES_CHANNEL_ID}"
  },
  "roles": {
    "verified": "${VERIFIED_ROLE_ID}",
    "unverified": "",
    "admin": "${ADMIN_ROLE_ID}",
    "staff": "${STAFF_ROLE_ID}",
    "owner": "${OWNER_ROLE_ID}",
    "vip": "",
    "member": "",
    "mod": "",
    "support": "",
    "supTickets": ""
  },
  "ticketSystem": {
    "logServerId": "",
    "logChannelId": null,
    "deleteInsteadOfArchive": true
  },
  "serverName": "IGNIS COMMUNITY",
  "embedColor": "#8B5FBF",
  "statusColor": "#7B68EE",
  "primaryColor": "#9932CC",
  "errorColor": "#ff0000",
  "website": {
    "baseUrl": "${BASE_URL}",
    "redirectUri": "${CALLBACK_URL}",
    "sessionSecret": "${SESSION_SECRET}",
    "production": {
      "baseUrl": "",
      "redirectUri": ""
    }
  }
}